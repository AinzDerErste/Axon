/**
 * Outbound links the app opens in the user's browser.
 *
 * Kept in one place because builds distributed outside GitHub (itch.io, for
 * example) should send people back to the page they downloaded from, not to
 * the GitHub releases list.
 */

/** Where a portable build points users to get a newer version by hand. */
export const RELEASES_URL = 'https://github.com/AinzDerErste/Axon/releases/latest'
